<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title }}</title>
  </head>
  <body>
    {{ content | safe }}

    {% for src in scripts %}
			<script src="{{ src }}" type="module"></script>
    {% endfor %}
  </body>
</html>
